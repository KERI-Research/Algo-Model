# Runtime dependencies for the professor deployment.
# Deliberately excludes PyTorch, XGBoost, ChromaDB, DoWhy and statsmodels:
# the deployment uses only the exported NumPy inference path.
fastapi>=0.115,<1
uvicorn>=0.30,<1
python-multipart>=0.0.9,<1
pandas>=2.2,<4
numpy>=1.26,<3
scikit-learn>=1.5,<2
joblib>=1.4,<2
